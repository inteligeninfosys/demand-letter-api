---
apiVersion: v1
kind: ConfigMap
metadata:
  name: demand-letter-config
  namespace: ecol
data:
  NODE_ENV: "production"
  TZ: "Africa/Nairobi"
  SOFFICE_PATH: "/usr/bin/soffice"
  # Match the Dockerfile; LO writes here (mounted as emptyDir below)
  LO_USER_INSTALLATION: "file:///tmp/lo_profile"
  # App-level defaults (override as needed)
  EMAIL_HOST: "smtp.office365.com"
  EMAIL_PORT: "587"
  EMAIL_SECURE: "false"
  EMAIL_USER: developer@sidianbank.co.ke
  EMAIL_FROM: "E-Collect <developer@sidianbank.co.ke>"
  MSSQL_SERVER: '192.168.12.73'
  MSSQL_PORT: '31433'
  MSSQL_DATABASE: 'ecolsid'
  MSSQL_USER: "sa"
  MSSQL_ENCRYPT: 'false'
  MSSQL_TRUST_SERVER_CERTIFICATE: 'true'
  OUR_REF_PREFIX: "SIDIAN/REC"
  S3_ENDPOINT: "http://minio-svc.ecol.svc.cluster.local:9000"
  S3_BUCKET: "ecol-demand-letters"
  S3_ACCESS_KEY: minioadmin
  S3_SECRET_KEY: minioadmin123
  S3_FORCE_PATH_STYLE: 'true'
  S3_SIGN_URL_EXP_SECONDS: '3600'
  S3_REGION: us-east-1
---
apiVersion: v1
kind: Secret
metadata:
  name: demand-letter-secret
  namespace: ecol-sid
type: Opaque
stringData:
  MSSQL_PASSWORD: "3.Collect"
  EMAIL_PASS: "$!M@"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: demand-api
  namespace: ecol-sid
  labels:
    app: demand-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: demand-api
  template:
    metadata:
      labels:
        app: demand-api
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
      containers:
        - name: demand-api
          image: 192.168.12.71:32000/demand-letters-api-sid:202601272049
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8004
              name: http
          envFrom:
            - configMapRef:
                name: demand-letter-config
            - secretRef:
                name: demand-letter-secret
          volumeMounts:
            - name: lo-profile
              mountPath: /tmp/lo_profile
          resources:
            requests:
              cpu: "200m"
              memory: "256Mi"
            limits:
              cpu: "1"
              memory: "1Gi"
      volumes:
        - name: lo-profile
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: demand-api-svc
  namespace: ecol-sid
  labels:
    app: demand-api-svc
spec:
  type: ClusterIP
  selector:
    app: demand-api-svc
  ports:
    - name: http
      port: 8004
      targetPort: http
---
kind: Ingress
metadata:
  name: demand-letter-ingress
  namespace: ecol-sid
spec:
  ingressClassName: nginx
  rules:
  - host: demands.sidianbank.co.ke
    http:
      paths:
      - backend:
          service:
            name: demand-api-svc
            port:
              number: 8004
        path: /
        pathType: Prefix
  tls:
  - hosts:
    - demands.sidianbank.co.ke
    secretName: ecollect-tls